import 'package:flutter/material.dart';

class RoutineAppBar extends StatelessWidget {
  const RoutineAppBar({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
        child: Image.asset(
      'assets/images/logo-WRA10.png',
    ));
  }
}
